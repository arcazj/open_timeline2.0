// Documentation-only UI studies. Does not load or modify the application.
import { chromium } from '@playwright/test';
import { icons } from 'lucide';
import { mkdir, writeFile, readFile } from 'node:fs/promises';
import { createHash } from 'node:crypto';
import { fileURLToPath } from 'node:url';
import path from 'node:path';
import assert from 'node:assert/strict';

const root = fileURLToPath(new URL('../', import.meta.url));
const output = path.join(root, 'docs/sorting-filtering');
await mkdir(output, { recursive: true });
const icon = name => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true">${icons[name].map(([tag, attributes]) => `<${tag} ${Object.entries(attributes).map(([key, value]) => `${key}="${value}"`).join(' ')}/>`).join('')}</svg>`;
const tool = (name, title) => `<button class="icon" title="${title}" aria-label="${title}">${icon(name)}</button>`;
function documentHtml(advanced) {
  return `<!doctype html><html lang="en"><meta charset="utf-8"><title>Proposed filter interface</title><style>
*{box-sizing:border-box;letter-spacing:0}body{margin:0;color:#243339;font:14px Arial,sans-serif;background:#fff}button,input,select{font:inherit;color:inherit}button,input,select{border:1px solid #c7ced1;border-radius:4px;background:white;min-height:34px;padding:6px 9px}button{cursor:pointer}svg{width:18px;height:18px;display:block}.icon{width:34px;height:34px;display:grid;place-items:center;padding:6px;flex:none}.study{height:30px;background:#254c43;color:#fff;padding:7px 18px;font-size:12px}header{height:62px;padding:12px 18px;display:flex;align-items:center;gap:18px;border-bottom:1px solid #cbd3d6}h1{font-size:19px;margin:0;white-space:nowrap}.source{font-size:12px;color:#53666e}.toolbar{height:55px;padding:10px 18px;display:flex;align-items:center;gap:9px;border-bottom:1px solid #d5dcdf}.toolbar label{display:flex;gap:7px;align-items:center;font-size:12px}.toolbar .search{display:flex;align-items:center;margin-left:auto;gap:5px}.toolbar input{width:142px}.tabs{height:42px;display:flex;align-items:stretch;gap:22px;padding:0 18px;border-bottom:1px solid #cbd3d6;font-size:13px}.tab{display:flex;gap:6px;align-items:center}.tab.active{border-bottom:3px solid #24715a;font-weight:bold}.state{margin-left:auto;display:flex;align-items:center;font-size:12px;color:#426552}.workspace{display:grid;grid-template-columns:minmax(0,1fr) 358px;height:calc(100vh - 189px)}main{display:flex;flex-direction:column;min-width:0}.range{display:flex;gap:12px;align-items:center;padding:10px 18px;border-bottom:1px solid #d9dfe1;font-size:12px;min-height:52px}.range strong{font-size:14px}.range .zoom{display:flex;gap:4px;margin-left:auto;align-items:center}.count{padding:10px 18px;font-size:12px;color:#4e626b;min-height:37px}.chart{flex:1;position:relative;background:#f8fafb;min-height:180px}.chart canvas{position:absolute;width:100%;height:100%;inset:0}.pager{display:flex;align-items:center;gap:9px;padding:8px 18px;border-top:1px solid #d0d9dc;font-size:12px}.pager .right{margin-left:auto}.overview-title{padding:7px 18px;background:#edf2f3;font-size:12px;display:flex;justify-content:space-between}.overview{position:relative;height:115px;background:#f3f6f7;flex:none}.overview canvas{width:100%;height:100%}.source-line{border-top:1px solid #d2dcdf;padding:8px 18px;font-size:11px;color:#596a71}aside{border-left:1px solid #cbd3d6;display:flex;flex-direction:column;min-width:0;background:#fff}.panel-title{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;border-bottom:1px solid #e0e4e6}.panel-title h2{font-size:18px;margin:0}.panel-tabs{display:flex;gap:16px;padding:0 16px;border-bottom:1px solid #d6dddf;height:43px}.panel-tabs span{padding-top:13px}.panel-tabs .chosen{border-bottom:3px solid #24715a;font-weight:bold}.panel-body{padding:16px;flex:1;overflow:auto}label.field{display:block;font-size:12px;margin-bottom:6px;font-weight:bold}.row{display:flex;gap:8px;margin:8px 0 18px}.row>*{min-width:0;flex:1}.rule{border-top:1px solid #e0e5e7;padding:15px 0}.rule-heading{display:flex;align-items:center;gap:9px;font-size:12px;margin-bottom:10px}.rule-heading strong{font-size:14px}.rule-heading .icon{margin-left:auto;width:27px;height:27px;min-height:27px}.rule input,.rule select{width:100%}.rule .row{margin:7px 0 0}.small{font-size:12px;color:#576a72}.add{display:flex;gap:8px;align-items:center;margin:9px 0 19px}.preview{border-top:1px solid #d5dddf;padding-top:16px}.preview h3{margin:0 0 13px;font-size:14px}.counts{display:flex;gap:24px}.counts strong{display:block;font-size:24px;font-weight:500}.counts span{font-size:11px;color:#576a72}.example{margin-top:15px;font-size:12px;line-height:1.7}.example mark{background:#ffdf64;color:#303536;padding:2px 3px}.panel-actions{display:flex;gap:8px;border-top:1px solid #d3dade;padding:12px 16px;flex-wrap:wrap}.primary{background:#236b55;border-color:#236b55;color:white}.panel-actions .icon{margin-left:auto}.regex{font:13px Consolas,monospace;width:100%;margin-top:7px}.flags{display:flex;gap:15px;font-size:12px;margin:12px 0}.flags input{min-height:0}.valid{color:#22644e;display:flex;align-items:center;gap:6px;margin:12px 0;font-size:12px}pre{background:#f1f4f5;border:1px solid #d5dddf;padding:12px;font:12px/1.6 Consolas,monospace;border-radius:4px;white-space:pre-wrap;overflow-wrap:anywhere}footer{font-size:11px;color:#627078}.mobile-only{display:none}
main,aside,.panel-body{min-height:0}.panel-title,.panel-tabs,.panel-actions{flex-shrink:0}
.panel-body{padding:12px 16px}.row{margin:8px 0 10px}.rule{padding:9px 0}.rule-heading{margin-bottom:7px}.add{margin:8px 0 12px}.preview{padding-top:12px}.preview h3{margin-bottom:8px}.example{margin-top:10px;line-height:1.45}
${advanced ? '' : '.example{display:none}'}
@media(max-width:640px){.study{height:30px;font-size:10px;padding:8px 10px}header{height:52px;padding:10px;gap:8px}h1{font-size:17px}.source{margin-left:auto;font-size:10px}.source .desktop-only{display:none}.toolbar{height:50px;padding:8px 10px;gap:6px}.toolbar .desktop-only{display:none}.toolbar .search{margin-left:0}.toolbar input{width:76px}.toolbar label select{max-width:105px}.tabs{padding:0 10px;gap:18px;font-size:12px;height:39px}.state{font-size:10px}.workspace{height:calc(100vh - 171px);display:block}main{display:none}aside{height:100%;border:0}.panel-title{padding:10px 14px}.panel-body{padding:12px 14px}.rule{padding:10px 0}.rule-heading{margin-bottom:7px}.row{margin-bottom:12px}.panel-tabs{height:38px}.panel-tabs span{padding-top:10px}.counts strong{font-size:22px}.example{margin-top:10px}.panel-actions{padding:10px 14px}.rule .row{margin-top:5px}.mobile-only{display:block}.preview{padding-top:12px}.add{margin-bottom:12px}}
</style><body><div class="study">DESIGN PROPOSAL / Not implemented / Generic sample records</div><header><h1>OpenBEXI Timeline</h1><div class="source">Server data <span class="desktop-only">/ Operations archive / Read-only legacy JSON</span></div></header><nav class="toolbar" aria-label="Timeline"><label class="desktop-only">Sources <select><option>SOURCE1 + SOURCE2</option></select></label><label>View <select><option>Operations</option></select></label><label class="desktop-only">Group <select><option>NAMESPACE</option></select></label><div class="search">${tool('Search','Find')}<input aria-label="Search" value="5_1; 0_3">${tool('ChevronUp','Previous finding')}${tool('ChevronDown','Next finding')}</div><span class="desktop-only">${tool('ListFilter','Filters')}</span><span class="desktop-only">${tool('Settings','Settings')}</span><span class="desktop-only">${tool('CircleHelp','Help and sharing')}</span></nav><div class="tabs"><span class="tab active">${icon('ChartNoAxesGantt')} Timeline</span><span class="tab">${icon('Table2')} Table</span><span class="state">2 sources / snapshot pinned</span></div><div class="workspace"><main><div class="range">${tool('CalendarDays','Date and time range')}<strong>18 March 2024</strong><span>UTC</span><div class="zoom">Auto <input type="checkbox" checked aria-label="Automatic scaling">${tool('Minus','Zoom out')}${tool('Plus','Zoom in')}${tool('Focus','Fit visible range')}</div></div><div class="count">6 filter results / 3 findings / 2 namespaces / 8 rows</div><div class="chart"><canvas id="main"></canvas></div><div class="pager">${tool('ChevronUp','Previous row page')}<span>Rows 1-8 of 8</span>${tool('ChevronDown','Next row page')}<span class="right">Scale: HOUR / Auto</span></div><div class="overview-title"><strong>Overview</strong><span>3 findings / 17-19 March / UTC</span></div><div class="overview"><canvas id="overview"></canvas></div><div class="source-line">Read-only source data / Applied view revision 12 / No pending source changes</div></main><aside><div class="panel-title"><h2>Filters and search</h2>${tool('X','Close filters')}</div><div class="panel-tabs"><span class="${advanced?'':'chosen'}">Simple</span><span class="${advanced?'chosen':''}">Advanced</span></div><div class="panel-body">${advanced ? `<label class="field">Title / Regular expression</label><input class="regex" value="^Activity_(5_1|0_3)$" aria-label="Pattern"><div class="flags"><label><input type="checkbox"> Ignore case</label><label><input type="checkbox"> Multiline</label></div><div class="valid">${icon('CircleCheck')} Valid / shared regex dialect</div><label class="field">Definition / version 2</label><pre>{
  "op": "regex",
  "field": "/title",
  "pattern": "^Activity_(5_1|0_3)$",
  "flags": [],
  "matchMode": "search"
}</pre><div class="small">Records independently</div>` : `<label class="field">Match</label><div class="row"><select><option>All conditions (AND)</option></select></div><div class="rule"><div class="rule-heading"><strong>Namespace</strong><span>Text</span>${tool('X','Remove namespace condition')}</div><select><option>is any of</option></select><div class="row"><input aria-label="Namespaces" value="SOURCE1, SOURCE2"></div></div><div class="rule"><div class="rule-heading"><strong>Status</strong><span>Text</span>${tool('X','Remove status condition')}</div><div class="row"><select><option>is not</option></select><input aria-label="Status" value="CANCELLED"></div></div><button class="add">${icon('Plus')} Add condition</button><label class="field">Session matching</label><div class="row"><select><option>Records independently</option></select></div>`}<div class="preview"><h3>Draft preview / current interval</h3><div class="counts"><div><strong>${advanced?'3':'6'}</strong><span>filter results</span></div><div><strong>3</strong><span>findings</span></div><div><strong>2</strong><span>groups</span></div></div><div class="example"><strong>${advanced?'Why this record matches':'Findings'}</strong><br><mark>Activity_5_1</mark> / SOURCE1<br>${advanced?'Title matches rule 1 / direct finding':'Activity_0_3 / SOURCE2'}</div></div></div><div class="panel-actions"><button class="primary">Apply</button><button>Cancel</button><button>Save view</button>${tool('Undo2','Undo last applied change')}</div></aside></div></body></html>`;
}

const browser = await chromium.launch();
const captures = [];
try {
  for (const [name, width, height, advanced] of [['simple',1360,860,false],['advanced',1360,860,true],['mobile',390,844,false]]) {
    const page = await browser.newPage({ viewport: { width, height }, deviceScaleFactor: 1 });
    await page.setContent(documentHtml(advanced));
    await page.evaluate(() => {
      for (const id of ['main','overview']) {
        const canvas = document.getElementById(id), box = canvas.getBoundingClientRect();
        canvas.width = Math.max(1,box.width); canvas.height = Math.max(1,box.height);
        if (box.width === 0) continue;
        const c = canvas.getContext('2d'), w = canvas.width, h = canvas.height, overview = id === 'overview';
        c.fillStyle = overview ? '#f3f6f7' : '#f8fafb'; c.fillRect(0,0,w,h);
        const tx = hours => 16 + (w - 32) * (overview ? hours / 72 : (hours - 32) / 8);
        c.fillStyle = '#eaae58'; c.globalAlpha = .18; c.fillRect(tx(34.7),0,tx(35.7)-tx(34.7),h-24); c.globalAlpha = 1;
        c.font = '12px Arial'; c.strokeStyle = '#dce3e7'; c.fillStyle = '#61737b';
        const divisions = overview ? 6 : 8;
        for(let i=0;i<=divisions;i++){const x=16+(w-32)*i/divisions;c.beginPath();c.moveTo(x,0);c.lineTo(x,h-23);c.stroke();c.fillText(overview?(i%2?'12:00':`${17+i/2} Mar`):`${String(8+i).padStart(2,'0')}:00`,Math.min(x+3,w-42),h-7);}
        if(overview){c.strokeStyle='#347767';c.lineWidth=2;c.strokeRect(tx(32),4,tx(40)-tx(32),h-31);for(const [x,y,len,col] of [[.19,25,.22,'#318265'],[.39,49,.19,'#8a5691'],[.62,68,.15,'#8a5691']]){c.fillStyle=col;c.fillRect(tx(32+x*8),y,tx(32+(x+len)*8)-tx(32+x*8),5);}return;}
        const groups = [{name:'SOURCE1',top:12,color:'#24765d'},{name:'SOURCE2',top:Math.floor(h*.53),color:'#85548e'}];
        for(const group of groups){c.fillStyle='#e6edef';c.fillRect(0,group.top,w,24);c.fillStyle='#334d58';c.font='bold 12px Arial';c.fillText(group.name,18,group.top+16);}
        const rows=[['Activity_5_1',.19,.22,52,true,0],['Telemetry check',.49,.16,98,false,0],['Review complete',.72,0,145,false,0],['Activity_0_3',.39,.19,Math.floor(h*.53)+42,true,1],['Activity_5_1',.62,.15,Math.floor(h*.53)+89,true,1],['Support window',.14,.2,Math.floor(h*.53)+132,false,1]];
        c.font='13px Arial';
        for(const [label,x,len,y,hit,g] of rows){const px=tx(32+x*8);if(hit){c.fillStyle='#ffdf64';c.fillRect(px-3,y-13,c.measureText(label).width+6,18);}c.fillStyle='#263a42';c.fillText(label,px,y);c.fillStyle=groups[g].color;if(len)c.fillRect(px,y+7,(w-32)*len,7);else{c.beginPath();c.arc(px-9,y-4,4,0,Math.PI*2);c.fill();}}
        c.fillStyle='#8a641f';c.font='11px Arial';c.fillText('Operations zone',w*.34+7,h-34);
      }
    });
    const overflow = await page.evaluate(() => document.documentElement.scrollWidth > innerWidth || document.documentElement.scrollHeight > innerHeight);
    assert.equal(overflow, false, `${name}: document overflow`);
    const clipped = await page.locator('button,select,input').evaluateAll(elements => elements.filter(el => el.getBoundingClientRect().width && el.scrollWidth > el.clientWidth + 2).map(el => el.outerHTML));
    assert.deepEqual(clipped, [], `${name}: clipped controls`);
    const previewVisible = await page.evaluate(() => {
      const panel = document.querySelector('.panel-body').getBoundingClientRect();
      return [...document.querySelectorAll('.counts,.example')].every(el => !el.offsetHeight || el.getBoundingClientRect().bottom <= panel.bottom);
    });
    assert.equal(previewVisible, true, `${name}: preview content below visible panel`);
    const file = `ui-${name}.png`;
    await page.screenshot({ path: path.join(output,file), fullPage: true });
    captures.push({file,width,height,sha256:createHash('sha256').update(await readFile(path.join(output,file))).digest('hex'),documentOverflow:false,clippedControls:0});
    await page.close();
  }
} finally { await browser.close(); }
await writeFile(path.join(output,'ui-proposals.json'), JSON.stringify({kind:'documentation-only design proposals',implementationVerified:false,captures},null,2)+'\n');
console.log('Captured three design proposals; no application behavior is certified.');
